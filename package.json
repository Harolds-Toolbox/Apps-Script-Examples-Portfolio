{
  "name": "apps-script-examples-portfolio-checks",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/validate-repo.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
