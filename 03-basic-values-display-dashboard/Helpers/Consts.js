// Sheet names and Script Properties normally adjusted per deployment.
const DASHBOARD_CONFIG = Object.freeze({
  reconciliationSheet: "Daily Reconciliation",
  countsSheet: "Operational Counts",
});

const DASHBOARD_PROPERTIES = Object.freeze({
  spreadsheetId: "DASHBOARD_SPREADSHEET_ID",
  authorizedEmails: "DASHBOARD_AUTHORIZED_EMAILS",
});
